---
phase: 02-trusted-ingestion-catalog-backbone
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
slice: "2c"
files_modified:
  - app/templates/discipline.html
  - app/templates/partials/upload_shell.html
  - app/static/app.css
  - app/api/routes/pages.py
  - tests/test_htmx_upload.py
autonomous: true
requirements:
  - UPL-01
  - WEB-03
user_setup:
  - Artefacts APIs + originals partials exist (**02-02**)
must_haves:
  truths:
    - Drag/drop zones post through HTMX (or graceful fallback submit) triggering upload endpoint.
    - Progress placeholders show optimistic state + swap to partial row list on completion.
---

<objective>

Complete **interactive originals UX**: drag/drop, HTMX swap targets, optimism/progress scaffolding per backlog without blocking ingestion backend.

Output: Operators can ingest multi-file bursts entirely from Browser UI.

</objective>

<execution_context>

@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/references/ui-brand.md

</execution_context>

<context>

@sdd.md §16 UX, @projekt-design-system.html tones (acadeMIND copy only)

</context>

<tasks>

<task type="auto">

<name>HTMX upload composer + swaps</name>

<files>app/templates/partials/upload_shell.html, discipline.html tab body</files>

<action>

Wire `hx-post` to `/api/upload` compatible endpoint or thin wrapper returning partial HTML per file for Originais tab; include `HX-Redirect`/`hx-swap` strategy documented.

</action>

<verify>Select-String "htmx" on templates</verify>

</task>

<task type="auto">

<name>CSS affordances</name>

<files>app/static/app.css</files>

<action>

Add dashed-drop states + focus-visible accessible cues matching dark shell.

</action>

</task>

<task type="auto">

<name>E2E-ish pytest via TestClient multipart + HX headers</name>

<files>tests/test_htmx_upload.py</files>

<action>

Simulate HX request headers verifying partial swaps return expected fragments.

</action>

<verify>python -m pytest -q</verify>

</task>

</tasks>

<verification>`python -m pytest -q` + manual drag/drop optional</verification>
