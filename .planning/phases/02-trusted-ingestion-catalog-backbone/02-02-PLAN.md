---
phase: 02-trusted-ingestion-catalog-backbone
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
slice: "2b"
files_modified:
  - app/models/jobs.py
  - app/repositories/jobs.py
  - app/services/job_service.py
  - app/api/routes/artefacts.py
  - app/logging_config.py
  - app/templates/partials/originals_list.html
  - app/templates/discipline.html
  - app/api/routes/pages.py
  - tests/test_catalogue_views.py
autonomous: true
requirements:
  - JOB-01
  - OBS-01
  - UPL-05
user_setup:
  - Wave **2a** complete (catalogue ingest live)
must_haves:
  truths:
    - Originals tab lists persisted artefacts from SQLite with metadata (name, size, hash prefix, bucket).
    - Destructive delete removes DB row + storage file guarded by discipline existence.
    - Upload path optionally creates job row pending → completed for correlation with OBS IDs.
---

<objective>

Surface **catalogue UX + JOB scaffolding + OBS hooks** aligned with originals tab requirements after bytes ingested in **02-01**.

Output: SSR partial swaps + optional download stub + structured logging keyed by ingest correlation ID.

</objective>

<execution_context>

@$HOME/.cursor/get-shit-done/workflows/execute-plan.md

</execution_context>

<context>

@.planning/phases/02-trusted-ingestion-catalog-backbone/02-CONTEXT.md
@.planning/REQUIREMENTS.md JOB-01, OBS-01, UPL-05

</context>

<tasks>

<task type="auto">

<name>Job ledger model + migrations</name>

<files>app/models/jobs.py, app/core/db.py (import hooks)</files>

<action>

Introduce `jobs` table with enums `pending|processing|completed|failed`, timestamps JSON diagnostics column, linkage to discipline + optional artefact FK, correlation UUID for logs.

</action>

<verify>python -m compileall app/models</verify>

</task>

<task type="auto">

<name>OBS-structured upload/log adapter</name>

<files>app/logging_config.py, new app/obs/logging.py</files>

<action>

Ensure upload handler logs JSON lines referencing correlation + job IDs into rotating `upload.log` per SDD taxonomy.

</action>

<verify>grep -r upload logs path OR pytest logger capture</verify>

</task>

<task type="auto">

<name>Originals listing + DELETE partial routes</name>

<files>app/api/routes/artefacts.py, app/templates/partials/originals_list.html, app/templates/discipline.html</files>

<action>

Expose `GET` JSON catalogue + SSR partial fragments; destructive delete guarded; never leak raw paths.

</action>

<verify>pytest coverage on delete + SSR contains artefact filenames</verify>

</task>

</tasks>

<verification>`python -m pytest -q`</verification>
