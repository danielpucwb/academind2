---
phase: 04-document-consolidation
plan: 01
type: execute
wave: 1
depends_on:
  - "03-01"
  - "02-01"
files_modified:
  - requirements.txt
  - pyproject.toml
  - app/core/paths.py
  - app/core/db.py
  - app/exceptions.py
  - app/services/pdf_merge_service.py
  - app/api/routes/merge.py
  - app/templates/discipline.html
  - app/templates/partials/processados_panel.html
  - app/static/app.css
  - app/main.py
  - tests/fixtures/pdfs/README.md
  - tests/test_pdf_merge.py
autonomous: true
requirements:
  - DOC-01
user_setup:
  - `pypdf` wheel install alongside existing FastAPI deps
must_haves:
  truths:
    - Merge honours alphabetical PDF ordering surfaced from disciplined catalogue/originals metadata.
    - Output lands atomically under `processados/pdfs/concatenado.pdf` with traversal anchors identical to ingestion.
    - Corrupt/unreadable contributors yield structured diagnostics surfaced to operators (PT-BR) without crashing worker.
    - Merge pathway acquires concurrency guard compatible with JOB-02 (integrated lock façade or PLAN-documented shim).
  artifacts:
    - app/services/pdf_merge_service.py
    - app/api/routes/merge.py
    - SSR + HTMX affordance on Processados scaffold
---

<objective>

Deliver **DOC-01**: alphabetical concatenation of discipline PDF originals into **`concatenado.pdf`**, visible from **Processados**, with deterministic filesystem placement + surfaced corruption telemetry.

Purpose: unblock Phase 5 export packaging that references merged PDF artefacts.

Output: Executable API + SSR hook + regression tests guarding merge contract.

</objective>

<execution_context>

@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-document-consolidation/04-CONTEXT.md
@.planning/phases/04-document-consolidation/04-RESEARCH.md
@sdd.md
@app/core/paths.py
@app/models/catalogue.py
@app/repositories/catalogue.py
@app/services/ingestion_service.py

</context>

<tasks>

<task type="auto">

<name>Add `pypdf` dependency pins</name>

<files>requirements.txt, pyproject.toml (optional commentary)</files>

<read_first>@.planning/research/STACK.md</read_first>

<action>

Pin **`pypdf`** consistent with STACK (`5.x` line). Smoke `import pypdf` post-install docs in README appendix optional (**skip unless README already referencing PDF merge**).

</action>

<verify>pip install -r requirements.txt ; python -c "import pypdf; print(pypdf.__version__)"</verify>

<done>Declared dependency merges safely with existing stack pins.</done>

</task>

<task type="auto">

<name>Concurrency guard façade + discipline resolution helpers</name>

<files>app/services/processados_lock.py, app/services/hierarchy_resolver.py</files>

<action>

Expose **`resolve_discipline_paths(session, discipline_id, settings)`** returning `(Course, Discipline, discipline_root Path)` guarded by repos (404 mapping later). Implement **`MergeLock`** protocol: `async with lock.for_discipline(uuid): …` Prefer **import shim** referencing Phase **`03-01`** exported symbol; fallback **POSIX lock file** + stale timeout with loud log warning until replaced.

</action>

<verify>python -m compileall app/services</verify>

</task>

<task type="auto">

<name>`PdfMergeService` orchestrator</name>

<files>app/services/pdf_merge_service.py</files>

<read_first>@app/repositories/catalogue.py, @04-RESEARCH.md</read_first>

<action>

Implement:

1. **Selection:** query catalogue rows belonging to discipline, filter **`pdf`** via extension + `bucket`/path prefix safeguards. Sort per CONTEXT. Empty input → deterministic `422` analogue exception class `NothingToMergeError`.  

2. **Validation:** refuse encrypted PDF (`pypdf` encryption flags) with surfaced code `encrypted_pdf`. Skip zero-page readers defensively.

3. **Merge loop:** sequential append; accumulate diagnostics on failures — **either** escalate to hard failure wiping temp **or** early abort policy consistent with CONTEXT (default hard fail DOC-01 best-effort messaging).

4. **Atomic write:** `concatenado.pdf.tmp-<uuid>` inside `discipline_root/processados/pdfs/` followed by **`os.replace`**. Afterwards `paths.assert_resolved_under_anchor`.

5. **Catalogue bookkeeping (optional PLAN fork):** either insert mirrored `catalogue_artefact` synthetic row flagged `processed_merge` OR persist dedicated `MergedDocument` stub — decision recorded in summary; minimal viable is **filesystem-only + API JSON** until duplicate detection needed.

Return structured DTO for router.

</action>

<verify>python -m compileall app/services/pdf_merge_service.py</verify>

</task>

<task type="auto">

<name>HTTP route + OpenAPI parity</name>

<files>app/api/routes/merge.py, app/main.py</files>

<action>

Register **`POST /api/disciplinas/{discipline_id}/merge-pdfs`**. Responses:

- **200 OK** `{ "ok": true, "merged": {...} , "diagnostics": [] }`  
- **409** when lock busy (**JOB contention**)  
- **422** `{ "ok": false, "reason": "...","diagnostics":[...]} ` for corrupt/invalid batch  
Attach router before static per existing convention if applicable.

Wire domain exceptions cleanly.

</action>

<verify>python -m compileall app/api/routes/merge.py</verify>

</task>

<task type="auto">

<name>Processados HTMX activation shell</name>

<files>app/templates/discipline.html, app/templates/partials/processados_panel.html, app/api/routes/pages.py</files>

<read_first>@projekt-design-system.html (tone cues only)</read_first>

<action>

Replace placeholder **Processados** body with actionable panel: explanatory copy citing future transcript listing + **Gerar concatenado.pdf** button posting via HTMX, swapping diagnostics partial. Portuguese strings; branding **acadeMIND** only.

</action>

<verify>Select-String -Path app/templates/discipline.html -Pattern 'concatenado'</verify>

</task>

<task type="auto">

<name>Regression tests</name>

<files>tests/fixtures/pdfs/*.pdf, tests/test_pdf_merge.py</files>

<action>

1. Fixtures: minimal two-page valid PDF binaries (can duplicate same bytes twice under different catalogue names exercising ordering).  

2. `TestClient` flow seed course/discipline, synthetic catalogue rows OR upload helper reusing ingestion route.  

3. Assertions: merges produce `stored_relative_path` output `processados/pdfs/concatenado.pdf`, ordering verified via embedded distinct marker bytes **or** page count heuristic.  

4. Negative tests: truncated fixture surfaces `422`/structured diagnostics; lock contention mocked via dependency override doubling acquisition.

</action>

<verify>python -m pytest -q</verify>

</task>

<task type="manual">

<name>Operator smoke on Windows workstation</name>

<files>n/a</files>

<action>

Manual `uvicorn` run: merge two dragged PDF originals, visually open `concatenado.pdf` verifying order + corruption toast when injecting broken file between valid ones.

</action>

</task>

</tasks>

<verification>

- `python -m compileall app`
- `python -m pytest -q`

</verification>

<success_criteria>

Phase 4 roadmap bullets satisfied in code + SSR affordance validating DOC-01 operator story.

</success_criteria>
