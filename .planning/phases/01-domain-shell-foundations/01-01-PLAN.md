---
phase: 01-domain-shell-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
  - app/__init__.py
  - app/main.py
  - app/core/config.py
  - app/core/db.py
  - app/core/paths.py
  - app/models/hierarchy.py
  - app/repositories/hierarchy.py
  - app/services/hierarchy_service.py
  - config.defaults.yaml
  - database/.gitkeep
autonomous: true
requirements:
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
  - ORG-05
user_setup:
  - Python 3.10+ venv with ability to pip install deps
must_haves:
  truths:
    - SQLModel Course/Discipline tables exist matching SDD identifiers (uuid pk + timestamps).
    - Repository + service helpers can create/rename/delete with slug rules and refuse breaking renames when files exist beneath discipline path.
    - SQLite file path respects config defaults and directory creation succeeds for `database/`.
  artifacts:
    - app/models/hierarchy.py
    - app/repositories/hierarchy.py
    - app/services/hierarchy_service.py
    - app/core/db.py
  key_links:
    - hierarchy_service resolves storage roots under `storage/cursos/{course_slug}/{discipline_slug}/`
---

<objective>

Bootstrap Python package skeleton, SQLite engine/session management, hierarchical domain models/repos/services implementing ORG behaviours (without HTTP/UI yet).

Purpose: Persist canonical metadata required before HTMX/HTML routes can mutate real user data safely.

Output: Runnable import surface (`python -c "from app.services.hierarchy_service import HierarchyService"`) verified by pytest-style smoke tests or inline asserts.

</objective>

<execution_context>

@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md
@.planning/phases/01-domain-shell-foundations/01-RESEARCH.md
@sdd.md

</context>

<tasks>

<task type="auto">

<name>Bootstrap packaging + configuration defaults</name>

<files>requirements.txt, pyproject.toml, config.defaults.yaml, app/__init__.py</files>

<read_first>@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md</read_first>

<action>

Add pinned dependencies reflecting `.planning/research/STACK.md` subset for skeleton: `fastapi`, `uvicorn[standard]`, `sqlmodel`, `python-slugify`, `pyyaml`, `pydantic-settings`, optional `pytest` + `httpx` for upcoming plans. Establish `pyproject.toml` `[project]` baseline with package `app`. Seed `config.defaults.yaml` documenting keys: `sqlite_path`, `storage_root`, `allowed_extensions`, `whisper_model`, `max_parallel_jobs`, sane defaults aligning SDD snippets. Loader implementation deferred but file must parse as valid YAML now.

</action>

<verify>python -c "import yaml; yaml.safe_load(open('config.defaults.yaml','r',encoding='utf-8'))"</verify>

<acceptance_criteria>

- Repository root contains syntactically valid `config.defaults.yaml` with documented keys enumerated in CONTEXT D-CONFIG.

</acceptance_criteria>

<done>YAML loads without error and dependencies listed for install.</done>

</task>

<task type="auto">

<name>Implement SQLModel persistence + repos</name>

<files>app/core/db.py, app/models/hierarchy.py, app/repositories/hierarchy.py</files>

<read_first>sdd.md, .planning/phases/01-domain-shell-foundations/01-CONTEXT.md</read_first>

<action>

Implement `Course`/`Discipline` SQLModel definitions with UUID primary keys + `nome`/`created_at` fields per SDD. Configure SQLite engine pointing to configurable path; enable WAL via `PRAGMA` hook. Provide session dependency creator `get_session()`. Implement repository functions: insert course, rename course guarded (if filesystem children exist disallow rename until deterministic policy—default block rename when `storage/cursos/{old_slug}` non-empty besides metadata), cascade delete stubs for discipline/course aligning ORG safeguards. Raise domain-specific exceptions surfaced later by API layer.

</action>

<verify>python -m compileall app</verify>

<acceptance_criteria>

- Modules import cleanly on Windows paths.

</acceptance_criteria>

<done>Persistence layer imports and compiles.</done>

</task>

<task type="auto">

<name>Add hierarchy service bridging DB + filesystem</name>

<files>app/services/hierarchy_service.py, app/core/paths.py, app/main.py</files>

<read_first>@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md</read_first>

<action>

`paths.py`: resolve `PROJECT_ROOT`, join `storage/cursos`, expose `ensure_discipline_paths(course_slug, discipline_slug)` creating `originais/{documentos,videos,audios,web}`, `processados/{transcricoes,pdfs,exports}`, `metadata` scaffolding per SDD even if unused until Phase 2. `HierarchyService`: `create_course` writes DB row + slug directory; symmetrical delete removes DB row then attempts `shutil.rmtree` guarded logging if failure; analogous discipline operations under parent slug. Minimal `FastAPI()` instance in `main.py` with placeholder route returning JSON `{"service":"phase1-bootstrap"}` is acceptable until Plan 02 replaces—ensures uvicorn smoke run.

Hooks only—no HTMX templates here.

</action>

<verify>python -c "from app.services.hierarchy_service import HierarchyService; print(HierarchyService)"</verify>

<acceptance_criteria>

- Importing HierarchyService succeeds after dependencies installed.

</acceptance_criteria>

<done>Service + stubs functional without manual intervention.</done>

</task>

</tasks>

<verification>

- Run `pip install -r requirements.txt`
- Run `python -m compileall app`
- Run `pytest` if tests added minimal smoke (optional placeholder skip)

</verification>

<success_criteria>

- Domain models persisted with slug helpers and directory scaffolding deterministic.
- ORG safeguards encoded in services ready for routers in Plan 02.
- Skeleton FastAPI factory importable (`from app.main import app`).

</success_criteria>
