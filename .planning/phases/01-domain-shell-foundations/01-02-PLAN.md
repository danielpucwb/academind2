---
phase: 01-domain-shell-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/main.py
  - app/api/routes/courses.py
  - app/api/routes/disciplines.py
  - app/api/__init__.py
  - app/templates/base.html
  - app/templates/dashboard.html
  - app/templates/discipline.html
  - app/templates/partials/course_rows.html
  - app/templates/partials/discipline_rows.html
  - app/static/app.css
autonomous: true
requirements:
  - API-01
  - API-02
  - WEB-01
  - WEB-02
user_setup: []
must_haves:
  truths:
    - HTML dashboard lists courses and links into discipline views with Portuguese tab labels scaffolded per CONTEXT.
    - REST endpoints mutate hierarchy via HierarchyService enforcing slug + validation errors surfaced as flash partials or inline errors when HTMX swaps return.
    - HTMX-enhanced POST forms persist create/delete discipline/course behaviours without manual SQLite editing.
  artifacts:
    - app/api/routes/courses.py
    - app/api/routes/disciplines.py
    - app/templates/*.html partials enumerated above
  key_links:
    - Routers wired in `FastAPI()` include `courses_router` prefix `/api/cursos`
---

<objective>

Expose HierarchyService behaviours through REST + SSR/HTMX UI so operators can onboard courses/disciplines visually while exercising API contracts API-01/02.

Purpose: Locks walking-skeleton UX flow prior to ingestion/processing complexities.

Output: Browser-navigable workflow hitting real FastAPI routers returning templates + JSON.

</objective>

<execution_context>

@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/PROJECT.md
@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md
@.planning/phases/01-domain-shell-foundations/01-RESEARCH.md

</context>

<tasks>

<task type="auto">

<name>Wire hierarchy REST routers</name>

<files>app/main.py, app/api/__init__.py, app/api/routes/courses.py, app/api/routes/disciplines.py</files>

<read_first>.planning/REQUIREMENTS.md, sdd.md</read_first>

<action>

Implement routers per SDD: `GET /api/cursos`, `POST /api/cursos`, `DELETE /api/cursos/{id}`, PATCH or PUT rename (choose explicit path) aligning ORG rename guard; analogous discipline routes including `POST /api/disciplinas` with JSON + form-compatible variants. Compose services + dependency-injected sessions. Raise HTTP 409 when rename/delete safeguards trip. Attach routers with tags for OpenAPI clarity.

Expose parallel HTML-facing routes `GET /` returning dashboard template context JSON + `POST /courses` hitting same service layers for HTMX (avoid duplicating logic—shared service functions callable from both REST + SSR handlers).

</action>

<verify>python -m compileall app</verify>

<acceptance_criteria>

- Routes registered on `FastAPI` instance introspectable (`[r.path for r in app.routes]` contains `/api/cursos`).

</acceptance_criteria>

<done>Routers compile and attach without import errors.</done>

</task>

<task type="auto">

<name>Create HTMX templates + static styling shell</name>

<files>app/templates/base.html, app/templates/dashboard.html, app/templates/discipline.html, app/templates/partials/course_rows.html, app/templates/partials/discipline_rows.html, app/static/app.css</files>

<read_first>@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md, projekt-design-system.html</read_first>

<action>

Implement `base.html` with meta viewport + Tailwind CDN + link to `/static/app.css`. `dashboard.html` lists courses (`WEB-01`) with buttons/forms using `hx-post` or classic POST to create/delete. `discipline.html` renders tab headers (**Originais · Processados · Processamento**) with placeholder bodies stating future phase scope (Portuguese strings). Provide partials swapped via HTMX for row updates avoiding full reload optional but recommended.

Keep branding textual references strictly **acadeMIND** matching PROJECT constraint.

</action>

<verify>powershell -Command "Select-String -Path app/templates/discipline.html -Pattern 'Originais'"</verify>

<acceptance_criteria>

- Template contains Portuguese tab labels exactly casing per CONTEXT decision.

</acceptance_criteria>

<done>Templates render via Jinja loader configured in FastAPI (`Jinja2Templates(directory='app/templates')`).</done>

</task>

<task type="auto">

<name>Add minimal ASGI regression smoke</name>

<files>tests/test_health_placeholder.py</files>

<read_first>@.planning/research/PITFALLS.md</read_first>

<action>

Create pytest using `TestClient` hitting `/api/cursos` baseline list (expect 200) after creating course via POST (201/200). Guards future regressions blocking HTMX routers. Skip if heavyweight—at least one asynchronous httpx asynchronous test verifying template route returns 200 with empty catalog.

Ensure `tests/__init__.py` exists.

</action>

<verify>pytest -q</verify>

<acceptance_criteria>

- CLI `pytest -q` passes with zero failures when dependencies installed.

</acceptance_criteria>

<done>Automated smoke green.</done>

</task>

</tasks>

<verification>

- `pytest -q`
- Manual `uvicorn app.main:app --reload` smoke by hitting `/` verifying forms appear (human optional if timeboxed)

</verification>

<success_criteria>

- Users can onboard hierarchy exclusively through UI/API per ORG/API success criteria scaffolding.
- HTMX/tab skeleton visible with Portuguese copy baseline.

</success_criteria>
