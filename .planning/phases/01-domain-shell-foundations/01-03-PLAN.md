---
phase: 01-domain-shell-foundations
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - app/main.py
  - app/core/config.py
  - app/api/routes/health.py
  - app/logging_config.py
  - config.defaults.yaml
  - README.md
autonomous: true
requirements:
  - API-06
  - CFG-01
user_setup:
  - Optional local `config.example.yaml` for operators (commit template only)
must_haves:
  truths:
    - Application refuses startup when required YAML keys missing or malformed (fail-fast with actionable stderr message).
    - `/health` returns JSON including SQLite connectivity, FFmpeg binary detection, Whisper/CUDA sentinel per CONTEXT (no weight download).
    - README documents how to duplicate `config.example.yaml` into `config.yaml` and launch `uvicorn app.main:app --reload`.
  artifacts:
    - app/core/config.py
    - app/api/routes/health.py
  key_links:
    - Lifespan hooks load validated settings shared by HierarchyService/storage helpers and health router
---

<objective>

Harden observability footprint — authoritative YAML-loaded settings powering storage/db paths across services; compose `/health` diagnostics; document bootstrap (README).

Purpose: Deterministic operational signals ahead of FFmpeg/Whisper-heavy phases.

Output: README + launcher instructions with predictable startup failure semantics.

</objective>

<execution_context>

@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/PROJECT.md
@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md

</context>

<tasks>

<task type="auto">

<name>Implement pydantic-backed YAML settings loader</name>

<files>app/core/config.py, config.defaults.yaml, config.example.yaml, app/main.py</files>

<read_first>@.planning/phases/01-domain-shell-foundations/01-CONTEXT.md</read_first>

<action>

Extend `Settings` model (subclass `BaseSettings` or custom dataclass + manual parse) merging optional user `config.yaml` over `config.defaults.yaml`. Required keys minimally: `sqlite_path`, `storage_root`, `ffmpeg_path` (defaults `ffmpeg`). On startup absence of file should copy defaults programmatically ONLY if README instructs copying — prefer explicit fail if user file missing (`FileNotFoundError`) **or** auto-load defaults from packaged string for dev friendliness documented in CONTEXT; pick **auto-merge defaults+custom overlay** documented in README. Wire `lifespan` to construct global `settings` singleton and log resolved absolute paths (`path.resolve()`).

Refactor HierarchyService/path helpers introduced in Plan 01 to consume injected settings rather than literals.

Expose `config.example.yaml` (tracked) reflecting final schema.

</action>

<verify>python -m compileall app</verify>

<acceptance_criteria>

- Loading settings twice yields identical canonical paths (`pathlib.Path` equality).

</acceptance_criteria>

<done>Settings module importable independent of routers.</done>

</task>

<task type="auto">

<name>Implement `/health` router + FFmpeg/torch.sentinel probes</name>

<files>app/api/routes/health.py, app/main.py</files>

<read_first>@.planning/phases/01-domain-shell-foundations/01-RESEARCH.md</read_first>

<action>

Expose `GET /health` assembling JSON `{ status, sqlite, ffmpeg, whisper_cuda }`:
- SQLite: execute `session.exec(text(\"SELECT 1\"))`.
- FFmpeg: subprocess version check bounded timeout — treat missing exe as `{available:false}` with `status:'degraded'`.
- Whisper CUDA sentinel: guarded import `torch` optional; evaluate `cuda.is_available()` when present else `null`. Never instantiate Whisper pipeline.

Ensure router registered before static mounts (prefix `/health` unstoppable vs `/static` overshadowing — verify Starlette routing order documented comment).

Extend pytest from Plan 02 to assert `/health` returns 200 and JSON keys exist.

</action>

<verify>pytest -q</verify>

<acceptance_criteria>

- Response schema keys always present string/bool/null values as described.

</acceptance_criteria>

<done>Automated assertion passes offline even when ffmpeg absent (still returns JSON with degraded flag).</done>

</task>

<task type="auto">

<name>Author README bootstrap + structured logging scaffold</name>

<files>README.md, app/logging_config.py</files>

<read_first>sdd.md</read_first>

<action>

README covers Windows PowerShell snippets: python venv, `pip install -r requirements.txt`, copy `config.example.yaml → config.yaml`, run `uvicorn app.main:app --reload`.

Add lightweight `logging_config.configure(settings)` configuring root logger INFO + optional rotating file handler if `logging_dir` set in YAML; create directory guarded.

Document `/health` usage for sanity checking GPU stack later.

</action>

<verify>powershell -Command "Select-String -Path README.md -Pattern 'uvicorn'"</verify>

<acceptance_criteria>

- README references `uvicorn` command explicitly.

</acceptance_criteria>

<done>Docs + logging hook invoked from lifespan.</done>

</task>

</tasks>

<verification>

- `pytest -q`
- Manual `curl http://127.0.0.1:8000/health | jq`

</verification>

<success_criteria>

- Operators can differentiate configuration failures vs subsystem degradation via `/health` JSON semantics.
- Settings drive storage + DB paths uniformly across phases.

</success_criteria>
