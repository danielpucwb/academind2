# PDF merge fixtures

Use `tests.test_pdf_merge` helpers to emit minimal `pypdf` writers for repeatable merge ordering checks.
Binary `.pdf` files may be dropped here once stable, but programmatic generation avoids blob drift across OSes.
